{
  "name": "iptv-hub",
  "version": "1.0.0",
  "description": "Free IPTV channel aggregator — static site with daily auto-update",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "serve": "npx serve ."
  },
  "engines": {
    "node": ">=18"
  }
}
